{
  "match_mode": {
    "description": "按哪个字段匹配提供商",
    "type": "string",
    "default": "base_url",
    "options": [
      "base_url",
      "provider_id",
      "provider_type"
    ],
    "labels": [
      "按 base_url（默认）",
      "按提供商 ID",
      "按提供商类型"
    ],
    "hint": "选择用哪个字段去匹配下面的关键字，三选一，只比这一个字段。base_url：提供商配置里的地址（严格；若请求经过你自己的中转，地址是内网 IP、与上游域名无关，会匹配不到）。provider_id：形如 opencode/deepseek-v4-flash，经过中转时通常仍带 opencode，**走自建中转请选这项**。provider_type：形如 chat_completion。"
  },
  "host_keywords": {
    "description": "匹配关键字",
    "type": "list",
    "default": [
      "opencode"
    ],
    "hint": "在上面选定的字段中查找这些关键字（不区分大小写），命中任意一个即注入该头。**留空表示不筛选，对所有提供商都注入。**"
  },
  "target_header": {
    "description": "注入的请求头名称",
    "type": "string",
    "default": "X-Opencode-Session",
    "hint": "默认 X-Opencode-Session。除非你对接的是别的兼容网关，否则不要改。注意大小写需要与上游要求完全一致：SDK 合并请求头时区分大小写，写成别的大小写会导致重复头而非覆盖。"
  }
}
